# Observability

This folder is reserved for Prometheus, Grafana and log pipeline configuration.

The services currently expose health checks and structured logs where practical. Metrics can be added without changing the core analysis flow.
