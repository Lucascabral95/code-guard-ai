# Docker Infrastructure

This folder is reserved for Docker-specific infrastructure assets that should not live inside an application folder.

The current local runtime is defined at the repository root in `docker-compose.yml`.
