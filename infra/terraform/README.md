# Terraform

Terraform is intentionally not implemented in v1.

The target deployment path is AWS ECS Fargate with managed PostgreSQL, managed Redis-compatible queues, container logs and a private network boundary between public and internal services.
